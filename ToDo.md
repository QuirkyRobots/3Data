# To do

* Add a privacy-coin detector indicator (green light)
* Add a spin-speed controller
* Add a swoosh sound for the panel sliding in
* Make note text wrap
* Fix heights and heading sizes
* Add glass options for the cube
* Make corners rounded
* Add music
* Make video and music stop when pause is pressed
* Make play/pause a single-word toggle
* Add a countdown timer
* Add a logo option instead of a note
* Create custom ZEC / ARRR / zk-SNARKs themes
* Add theme backgrounds

* Add a plain-text stats option
* Add multiple cubes
* Make slide-in popups to advertise zk-SNARKs
* Show values next to value titles
* Disable backgrounds for themes, or remove the theme when options are changed, or add a theme parameter to the URL

# Bugs / improvements

* Note / MSG not updating when a new coin is selected from the search
* ATH appears to be incorrect
* `will-change` needs adding to some CSS elements
* Note field should display "note"
* Wireframe should be a slider only (no checkbox)
* Update coin after selecting predictive text suggestion
* CORS-denied access causes a note update bug